Project Analysis and Debugging Notes

General Logic and Flow:
- Explain how the 2D character array works as a canvas. Is it efficient to clear and redraw everything every time a shape is added, or is there a better way to do it for a terminal app?
- Give me a step-by-step trace of what happens when the user picks option 1 to add a circle. Start from main() and go through draw_circle and put_pixel.
- Looking at the Shape struct, is there a way to handle the extra variables more efficiently? Like, why do we need x3 and y3 for a line, or radius for a triangle? Should we use a union instead?

Algorithm Specifics (CG Labs):
- Explain the Bresenham's implementation in draw_line. How does the error variable calculation (2*err) relate to the mathematical derivation we see in textbooks?
- For the Midpoint Circle algorithm, why do we only calculate one octant and then use 8-way symmetry? Show the math for how (xc + x, yc + y) relates to the other 7 points.
- If I change the canvas size to 100x100, will these algorithms still work, or is something hardcoded that will break?

Memory and Array Management:
- We're using MAX_SHAPES = 100. What happens if the code reaches 101? Could we use pointers and dynamic memory (malloc) to make this unlimited?
- Is there any risk of a buffer overflow when inputting coordinates in scanf? How do we stop the user from typing a coordinate like (200, 200)?

Possible Improvements for the Lab:
- How can I add a feature to save the current 'canvas' state into a permanent text file and then load it back later?
- Right now it just uses '*', can we modify the struct so each shape can have its own character (like '#', '@', or '+')?
- How would you implement a simple 'undo' feature that deletes the last shape added to the shapes array?

Visualisation:
- Make a flowchart for the rendering loop (render_all) that shows how the switch-case handles different ShapeTypes.
- Can you write a small script to take the test_output.txt and turn it into a black and white image so I can see the shapes better?
- Explain how we could use a library like ncurses to make the menu looks more professional instead of just printing text in a loop.
